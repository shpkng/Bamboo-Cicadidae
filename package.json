{
  "name": "cicadidae-motion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "cert:generate": "node scripts/generate-lan-cert.mjs",
    "dev": "vite",
    "dev:https": "npm run cert:generate && vite --host 0.0.0.0 --port 5174",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "selfsigned": "^5.5.0",
    "typescript": "^5.8.3",
    "vite": "^7.1.7"
  }
}
